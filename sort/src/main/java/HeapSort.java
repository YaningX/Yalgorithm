/**
 * Little Heap: in an array numbers[], numbers[i] <= numbers[2 * i] && numbers[i] <= numbers[2 * i + 1]
 */
public class HeapSort {
    public static int[] heapSort(int[] numbers) {
        int[] resArray = numbers.clone();

        return resArray;
    }
}
